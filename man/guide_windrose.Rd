% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guides.R
\name{guide_windrose}
\alias{guide_windrose}
\title{Legend guide with automatic calm-wind footer}
\usage{
guide_windrose(calm_text = NULL, title = ggplot2::waiver(), ...)
}
\arguments{
\item{calm_text}{\code{character(1)} or \code{NULL}. Footer text. When \code{NULL}
(default), the calm percentage is extracted automatically from
the \code{StatWindrose} layer data.}

\item{title}{\code{character(1)} or \code{\link[ggplot2:waiver]{ggplot2::waiver()}}. Legend title.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:guide_legend]{ggplot2::guide_legend()}}.}
}
\value{
A \code{GuideWindrose} ggproto object.
}
\description{
Extends \code{\link[ggplot2:guide_legend]{ggplot2::guide_legend()}} to render the calm-wind percentage
below the legend colour boxes.
}
\details{
The calm percentage is extracted \strong{automatically} from the
\code{StatWindrose} layer data — no need to compute or pass it manually.
Just use \code{guide_windrose()} with no arguments.

You may still override the footer text via \code{calm_text} if needed
(e.g. for a translated label or a different threshold).

Requires \strong{ggplot2 >= 3.5.0}.
}
\examples{
library(ggplot2)
set.seed(1)
df <- data.frame(wdir = sample(0:359, 300, replace = TRUE),
                 wspd = c(rep(0, 20), rexp(280, 0.2)))

# Calm \% added automatically — no df$wspd needed
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose(name = "Speed (m/s)") +
  theme_windrose()

# Override the footer label (e.g. French)
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose(
    name  = "Vitesse (m/s)",
    guide = guide_windrose(calm_text = windrose_calm_pct(df$wspd,
                                                         label = "Calme : "))
  ) +
  theme_windrose()

}
\seealso{
\code{\link[=scale_fill_windrose]{scale_fill_windrose()}}, \code{\link[=windrose_calm_pct]{windrose_calm_pct()}}
}
