% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_windrose.R
\name{stat_windrose}
\alias{stat_windrose}
\title{Wind rose stat}
\usage{
stat_windrose(
  mapping = NULL,
  data = NULL,
  geom = "col",
  position = "stack",
  ...,
  n_dir = 16,
  speed_breaks = NULL,
  speed_labels = NULL,
  calm_threshold = 0.5,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. At minimum:
\code{aes(x = wdir, y = wspd)}. \code{fill} defaults to
\code{after_stat(speed_class)} unless overridden.}

\item{data}{A data frame. \code{NULL} inherits from \code{ggplot()}.}

\item{geom}{Geom to use. Default \code{"col"}.}

\item{position}{Position adjustment. Default \code{"stack"}.}

\item{...}{Extra parameters passed to \code{GeomWindrose} or
\code{StatWindrose}.}

\item{n_dir}{Number of direction sectors. Default \code{16} (22.5°).
Use \code{8} for 45° sectors.}

\item{speed_breaks}{Numeric vector of speed bin edges, or \code{NULL} for the
default meteorological progression: \verb{c(0.5, 1, 2, 4, 6, 8, 10, …, Inf)}.
The last element may be \code{Inf} for an open-ended bin.}

\item{speed_labels}{Character vector of bin labels, length
\code{length(speed_breaks) - 1}. Auto-generated when \code{NULL}.}

\item{calm_threshold}{Observations with speed \eqn{\le} this value are
treated as calm. Default \code{0.5}.}

\item{na.rm}{Drop \code{NA} values silently? Default \code{FALSE}.}

\item{show.legend}{Logical; \code{NA} includes in legend if mapped.}

\item{inherit.aes}{Inherit aesthetics from \code{ggplot()}? Default \code{TRUE}.}
}
\description{
Bins wind direction (\code{x}) and speed (\code{y}) into a direction-by-speed
frequency table. Calm winds (speed \eqn{\le} \code{calm_threshold}) are
excluded from the bars but kept in the denominator.
}
\details{
This stat is used internally by \code{\link[=geom_windrose]{geom_windrose()}}. Use \code{\link[=stat_windrose]{stat_windrose()}}
directly only when you need a non-default geom.
}
\section{Computed variables}{

\describe{
\item{\code{after_stat(y)}}{Frequency as \% of all observations (incl. calm).}
\item{\code{after_stat(speed_class)}}{Ordered factor of speed bin labels.}
\item{\code{after_stat(calm_pct)}}{Calm percentage, constant across the panel.}
}
}

\examples{
library(ggplot2)
set.seed(1)
df <- data.frame(wdir = sample(0:359, 300, replace = TRUE),
                 wspd = c(rep(0, 20), rexp(280, 0.2)))

# Access calm_pct via after_stat()
ggplot(df, aes(x = wdir, y = wspd)) +
  stat_windrose(aes(fill = after_stat(speed_class)), width = 22.5) +
  coord_windrose() +
  scale_x_windrose()

}
