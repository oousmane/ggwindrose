% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_windrose.R
\name{theme_windrose}
\alias{theme_windrose}
\title{Minimal theme adjustments for wind rose plots}
\usage{
theme_windrose()
}
\value{
A \code{theme} object.
}
\description{
Returns a \code{\link[ggplot2:theme]{ggplot2::theme()}} object that:
\itemize{
\item removes axis titles and y-axis text / ticks
\item makes compass labels bold
}
}
\details{
Add with \code{+ theme_windrose()}. No theme is applied by default inside
any \code{ggwindrose} function.
}
\examples{
library(ggplot2)
set.seed(1)
df <- data.frame(wdir = sample(0:359, 300, replace = TRUE),
                 wspd = rexp(300, 0.2))

# theme_windrose() alone
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose() +
  theme_windrose()

# Combined with a base theme (order matters: base first, then windrose)
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose() +
  theme_minimal() +
  theme_windrose()

}
