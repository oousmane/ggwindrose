% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_windrose.R
\name{geom_windrose}
\alias{geom_windrose}
\title{Wind rose layer}
\usage{
geom_windrose(
  mapping = NULL,
  data = NULL,
  position = "stack",
  ...,
  n_dir = 16,
  speed_breaks = NULL,
  speed_labels = NULL,
  calm_threshold = 0.5,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. At minimum:
\code{aes(x = wdir, y = wspd)}. \code{fill} defaults to
\code{after_stat(speed_class)} unless overridden.}

\item{data}{A data frame. \code{NULL} inherits from \code{ggplot()}.}

\item{position}{Position adjustment. Default \code{"stack"}.}

\item{...}{Extra parameters passed to \code{GeomWindrose} or
\code{StatWindrose}.}

\item{n_dir}{Number of direction sectors. Default \code{16} (22.5°).
Use \code{8} for 45° sectors.}

\item{speed_breaks}{Numeric vector of speed bin edges, or \code{NULL} for the
default meteorological progression: \verb{c(0.5, 1, 2, 4, 6, 8, 10, …, Inf)}.
The last element may be \code{Inf} for an open-ended bin.}

\item{speed_labels}{Character vector of bin labels, length
\code{length(speed_breaks) - 1}. Auto-generated when \code{NULL}.}

\item{calm_threshold}{Observations with speed \eqn{\le} this value are
treated as calm. Default \code{0.5}.}

\item{na.rm}{Drop \code{NA} values silently? Default \code{FALSE}.}

\item{show.legend}{Logical; \code{NA} includes in legend if mapped.}

\item{inherit.aes}{Inherit aesthetics from \code{ggplot()}? Default \code{TRUE}.}
}
\value{
A \code{ggplot2} layer.
}
\description{
Draws a wind rose by binning wind direction (\code{x}) and speed (\code{y}) into
stacked frequency bars. Calm winds (speed \eqn{\le} \code{calm_threshold})
are excluded from the bars but counted in the frequency denominator.
}
\details{
No theme is applied. Add \code{\link[=theme_windrose]{theme_windrose()}} or any \verb{+ theme_*()}.
}
\examples{
library(ggplot2)
set.seed(42)
df <- data.frame(
  wdir = sample(0:359, 500, replace = TRUE),
  wspd = c(rep(0, 30), rexp(470, 0.2))
)

# Basic wind rose — 16 sectors, default speed breaks
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose(
    name  = "Speed (m/s)",
    guide = guide_windrose(calm_text = windrose_calm_pct(df$wspd))
  )

# 8 sectors with a base theme
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose(n_dir = 8) +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose(
    name  = "Speed (m/s)",
    guide = guide_windrose(calm_text = windrose_calm_pct(df$wspd))
  ) +
  theme_minimal() +
  theme_windrose()

# Faceted by season (each panel has its own calm \%)
df$season <- sample(c("DJF", "MAM", "JJA", "SON"), 500, replace = TRUE)
ggplot(df, aes(x = wdir, y = wspd)) +
  geom_windrose() +
  coord_windrose() +
  scale_x_windrose() +
  scale_fill_windrose() +
  facet_wrap(~season) +
  theme_minimal() +
  theme_windrose()

}
\seealso{
\code{\link[=stat_windrose]{stat_windrose()}}, \code{\link[=coord_windrose]{coord_windrose()}}, \code{\link[=scale_x_windrose]{scale_x_windrose()}},
\code{\link[=scale_fill_windrose]{scale_fill_windrose()}}, \code{\link[=guide_windrose]{guide_windrose()}}, \code{\link[=windrose_calm_pct]{windrose_calm_pct()}},
\code{\link[=theme_windrose]{theme_windrose()}}
}
